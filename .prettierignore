# Prettier has no SQL parser installed, and the migrations are written to be
# read as SQL rather than reformatted by a JavaScript tool. `format:check`
# used to include them and could therefore never pass.
*.sql

.next
node_modules
playwright-report
test-results
coverage

# Per-machine tool state, not source. It is gitignored, but only by a global
# ignore file on some machines — Prettier scans the working directory rather
# than the index, so without this `format:check` fails locally for anyone whose
# editor has written one.
.claude/settings.local.json
